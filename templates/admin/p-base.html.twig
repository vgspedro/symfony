<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{ company.name }}</title>
        <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/w3-4.css') }}">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"/>
        <style>html,body,h1,h2,h3,h4,h5,p,a {font-family: "Raleway", sans-serif}</style>
        <script src="{{ asset('js/jquery-2.1.1.min.js') }}"></script>
    </head>

    <body class="w3-light-grey">
        <input type='hidden' name='stripe-pk' value='{{company.stripePk}}'>
        {% if host %} 
            <div style="position:fixed;top:36px;left:calc(50% - 43px);z-index:999999999999999" class="blink w3-xxxlarge w3-text-red"> Demo </div>
        {% endif %}

        {% include 'admin/header.html' %}

        <div id="content">{% block content %}{% endblock %}</div>
        
        <div id="modal-error" class="w3-modal" style="z-index: 9999999;padding-top:100px; padding-bottom: 32px">
          <div class="w3-modal-content w3-animate-zoom" style="max-width:400px">
            <div class="w3-container w3-red w3-padding">
              <span onclick="$('#modal-error').hide()" class="w3-padding w3-button w3-display-topright">
                <i class="w3-xlarge fa fa-times"></i>
              </span>
              <h2>
                <i class="fa fa-exclamation-triangle"></i>
                <span>Erro</span>
              </h2>
            </div>
            <div class="w3-container w3-text-black w3-large w3-padding">Verifique ligação internet e tente novamente!
            </div>
          </div>
        </div>
    </body>

</html>

    <script>
        function w3_open() {
            $("#side-bar").hasClass('w3-hide') ? $("#side-bar").removeClass('w3-hide') : $("#side-bar").addClass('w3-hide')
        }

        function navigation(){
            $("html, body").animate({
                scrollTop: $('#form').offset().top -125 }, 750)
        }
    </script>